buildPack: go
pipelineConfig:
  pipelines:
    pullRequest:
      postBuild:
        preSteps:
        - command: echo "======================= CuSTOM Postbuild pre  ======================"
        steps:
        - command: echo "======================= CUSTOM Postbuild post ======================"
